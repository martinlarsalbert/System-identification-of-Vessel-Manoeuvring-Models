
{% for ship in ships %} # (vmm defined in globals.yml)

  {% for vmm in vmms %} # (vmm defined in globals.yml)

    {% for id in joins %}  # (runs defined in runs_globals.yml)


{{ ship }}.{{ vmm }}.{{ id }}.derivatives:
  type: pandas.CSVDataSet
  filepath: data/06_models/{{ ship }}/{{ vmm }}/{{ id }}/{{ ship }}.{{ vmm }}.{{ id }}.derivatives.csv
  load_args:
    index_col: 0
  save_args:
    index: True

{{ ship }}.{{ vmm }}.{{ id }}.regression:
  type: pickle.PickleDataSet
  filepath: data/06_models/{{ ship }}/{{ vmm }}/{{ id }}/{{ ship }}.{{ vmm }}.{{ id }}.regression.pkl
  backend: dill
  
{{ ship }}.{{ vmm }}.{{ id }}.summary_X:
  type: text.TextDataSet
  filepath: data/06_models/{{ ship }}/{{ vmm }}/{{ id }}/{{ ship }}.{{ vmm }}.{{ id }}.summary_X.txt

{{ ship }}.{{ vmm }}.{{ id }}.summary_Y:
  type: text.TextDataSet
  filepath: data/06_models/{{ ship }}/{{ vmm }}/{{ id }}/{{ ship }}.{{ vmm }}.{{ id }}.summary_Y.txt

{{ ship }}.{{ vmm }}.{{ id }}.summary_N:
  type: text.TextDataSet
  filepath: data/06_models/{{ ship }}/{{ vmm }}/{{ id }}/{{ ship }}.{{ vmm }}.{{ id }}.summary_N.txt

{{ ship }}.{{ vmm }}.{{ id }}.plot_X:
  type: matplotlib.MatplotlibWriter
  filepath: data/06_models/{{ ship }}/{{ vmm }}/{{ id }}/{{ ship }}.{{ vmm }}.{{ id }}.plot_X.png

{{ ship }}.{{ vmm }}.{{ id }}.plot_Y:
  type: matplotlib.MatplotlibWriter
  filepath: data/06_models/{{ ship }}/{{ vmm }}/{{ id }}/{{ ship }}.{{ vmm }}.{{ id }}.plot_Y.png

{{ ship }}.{{ vmm }}.{{ id }}.plot_N:
  type: matplotlib.MatplotlibWriter
  filepath: data/06_models/{{ ship }}/{{ vmm }}/{{ id }}/{{ ship }}.{{ vmm }}.{{ id }}.plot_N.png

{{ ship }}.{{ vmm }}.{{ id }}.model:
  type: pickle.PickleDataSet
  filepath: data/06_models/{{ ship }}/{{ vmm }}/{{ id }}/{{ ship }}.{{ vmm }}.{{ id }}.model.pkl
  backend: dill
  
    {% endfor %}
  {% endfor %}
{% endfor %}